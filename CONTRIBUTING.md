# Contributing

Use Python 3.12 or newer, create a virtual environment, and install `.[dev]`. Run `python scripts/verify.py` before opening a pull request. Keep changes deterministic, add tests for behavior changes, and do not add user data or secrets to the repository.
